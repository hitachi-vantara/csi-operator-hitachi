apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-sample-block
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 1Gi
  storageClassName: sc-sample
